/*
Button in Input that opens this dialog box modal and this takes a url that on submit sends to Input.js to be sent in reqBody as attachment, so if attachment exists send it, etc.

Look into material-ui icons for a button to put in Input?
*/
